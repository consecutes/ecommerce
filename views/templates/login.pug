doctype html
html(lang="en")
	head
		include head.pug
		<!-- CSS PROPIO-->
		link(rel="stylesheet", href="/public/css/login.css")
		link(rel="stylesheet", href="/public/css/animations.css")

	body
		div(class="container")
			div(class="row")
				div(class="border shadow bg-white" id="login")
					div(class="col-12")
						h1(class="text-center mb-3 font-italic")
							strong Iniciar Sesión
					div(class="col-12 my-2 text-center")
						a(href="#" class="text-info")
							i(class="fab fa-facebook fa-2x")
						a(href="#" class="text-danger mx-3")
							i(class="fab fa-google fa-2x")
						a(href="#" class="text-info")
							i(class="fab fa-linkedin fa-2x")
					div(class="col-12 text-center")
						p(class="text-muted lead")
							em  o puedes usar tu correo
					form(action="/login" method="post")
						div(class="col-12")
							input(
								type="email",
								placeholder="Email",
								name="email"
								class="form-control mb-3 pl-3 mx-auto font-italic"
							)
							input(
								type="text",
								placeholder="Password",
								name="password"
								maxlength="12"
								class="form-control mb-3 pl-3 mx-auto font-italic"
							)
						div(class="col-12 mb-3", class="text-center")
							a(href="#")
								strong ¿Olvidaste tu contraseña?
						div(class="col-12")
							input(
								type="submit"
								class="btn btn-danger rounded-pill",
								id="iniciarSesionBoton"
								value="¡Iniciar sesión!"
								style="font-size:100%"
							)
					div(class="col-12 mt-4")
						p(class="text-muted") ¿Aún no tienes una cuenta? 
							a(href="#", id="signLink")
								strong Registrate aquí
		div(class="container d-none")
			div(class="row")
				div(class="border shadow bg-white" id="signUp")
					div(class="col-12")
						h1(class="text-center mb-3 font-italic")
							strong Registrate
					div(class="col-12 text-center")
						p(class="text-muted lead")
							em  Es gratis y solo toma un minuto :)
					div(class="col-12 form-group")
						form(action="/login" method="post")
							div(class="row")
								div(class="col-12" style="padding-left:0px;")
									input(
										type="text",
										placeholder="Primer Nombre",
										name="firstName"
										class="form-control mb-3 d-inline pl-3 font-italic"
										id="firstName"
										required
									)
									input(
										type="text",
										placeholder="Apellido",
										name="lastName"
										class="form-control mb-3 float-right pl-3 d-inline font-italic"
										id="lastName"
										required
									)
							div(class="row")
								div(class="col-12")
									input(
										type="text",
										placeholder="Correo Eléctronico",
										name="email"
										class="form-control mb-3 float-right pl-3 font-italic"
										required
									)
							div(class="row")
								div(class="col-12")
									input(
										type="text",
										placeholder="Contraseña",
										name="password"
										maxlength="12"
										class="form-control mb-3 float-right pl-3 font-italic"
										required
									)
							div(class="row")
								div(class="col-12")
									input(
										type="text",
										placeholder="Confirma contraseña",
										maxlength="12"
										class="form-control mb-3 float-right pl-3 font-italic"
										required
									)
							div(class="row mt-1")
								div(class="col-12 pl-0")
									div(class="form-check")
										input(
											type="checkbox"
											name="terms"
											class="form-check-input mb-3"
											required
										)
										label(class="form-check-label")
											p(class="text-muted d-inline") Acepto los 
												a(href="#") términos de uso 
											p(class="text-muted d-inline") y 
												a(href="#") política de privacidad
							div(class="row mt-2")
								input(
									type="submit"
									class="btn btn-danger rounded-pill mx-auto",
									id="iniciarSesionBoton"
									value="¡Registrarme ahora!"
									style="font-size:100%"
								)
					div(class="col-12 mt-4 text-center")
						p(class="text-muted") Si ya tienes una cuenta  
							a(href="#", id="loginLink")
								strong inicia sesión

		script(type="module", src="/public/js/login.js")